{
  "name": "mijija-live",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "preview:smoke": "node scripts/review/preview-smoke.mjs",
    "ui:capture": "node scripts/review/capture-ui.mjs --out docs/ui-review/after",
    "firebase:login": "firebase login",
    "firebase:use": "firebase use --add",
    "firebase:deploy": "firebase deploy --only firestore:rules,firestore:indexes",
    "firebase:deploy:live": "firebase deploy --project live --only firestore:rules,firestore:indexes",
    "firebase:deploy:test": "firebase deploy --project test --only firestore:rules,firestore:indexes",
    "firebase:deploy:storage": "firebase deploy --only storage",
    "firebase:deploy:storage:live": "firebase deploy --project live --only storage",
    "firebase:deploy:storage:test": "firebase deploy --project test --only storage",
    "firebase:check-env": "node scripts/firebase/check-env.mjs"
  },
  "dependencies": {
    "firebase": "^12.4.0",
    "firebase-admin": "^13.8.0",
    "motion": "^12.38.0",
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "firebase-tools": "^15.15.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
