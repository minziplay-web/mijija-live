# Scripts

Repo scripts are grouped by purpose:

- `review/`: local preview routes, smoke checks, and UI screenshot capture.
- `firebase/`: Firebase environment checks and one-off maintenance helpers.

Prefer package scripts from `package.json` for common tasks.
